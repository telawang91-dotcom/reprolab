# ReproLab 安全说明

## 支持范围

当前维护分支是 `main`。ReproLab 现阶段定位为单机科研工作台、受信任团队内部 Beta
和演示系统，不承诺未经加固即可作为公网多租户服务运行。

## 部署前必须完成

1. 不提交 `.env`、模型密钥、研究数据、数据库备份、内容存储或复现导出包。
2. 任何可被其他人访问的 Agent API 都必须设置强随机 `AGENT_API_TOKEN`。
3. 不把 `SANDBOX_BACKEND=host` 暴露给不受信任的请求；对外环境使用独立执行节点、
   网络出口限制、资源配额和操作系统级隔离。
4. 在反向代理层启用身份认证、TLS、请求体限制、超时、速率限制和访问日志。
5. 将 PostgreSQL、对象存储和配置卷纳入同一备份与访问控制边界。

## 当前已知边界

- 站内接口在 demo 阶段没有完整用户认证和角色权限。
- 项目隔离是应用层作用域，不是独立租户或独立数据库安全边界。
- FastAPI BackgroundTasks 和进程内任务表不提供跨重启持久队列保证。
- Docker/Jupyter 沙箱面向本机可信使用者，不是敌对代码的完整安全沙箱。
- 远程模型调用会把完成任务所需的提示、字段或证据片段发送到所配置服务。

## 依赖审计状态

2026-08-09 的本地审计已将项目直接使用的 PostCSS 更新到 `8.5.23`，并将 Nanoid
解析到 `3.3.18`。`npm audit --omit=dev` 仍对锁定的 Next.js `14.2.x` 报告高危公告，
自动修复会跨越到 Next.js 16，违反当前技术栈兼容约束。完成有计划的框架迁移和完整
回归前，不应把当前 Next.js 服务直接暴露为无代理、无认证的公网应用。

重新审计：

```powershell
.\.venv\Scripts\python.exe -m pip check
npm.cmd --prefix frontend audit --omit=dev
```

## 报告安全问题

请优先使用 GitHub 的私有漏洞报告功能或私下联系仓库维护者。不要在公开 Issue 中附带
真实密钥、研究数据、可识别个人信息、未脱敏日志或可直接利用的完整攻击载荷。

报告中建议包含：受影响版本、部署方式、复现条件、影响范围、最小复现和建议缓解措施。
