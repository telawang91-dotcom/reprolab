## 〖第 8 份〗 文件路径：docs/06-DESIGN.md

# 06・UI / UX 设计规范

开发所有前端页面、组件前必读；目标：专业科研工具产品质感，规避业余粗糙交互。

### 技术栈

shadcn/ui + TailwindCSS + lucide 图标 + reactflow（溯源图谱）+ react-markdown/katex（公式富文本）+ framer-motion（交互动效）

## 0. 视觉北极星（统一对标产品）

- 外壳布局、极简克制风格：Linear
- 引用 / 数字锚点交互、弹窗来源卡片：Perplexity

### 对标细节拆解

1. Linear 借鉴点：左侧固定窄导航、全局 Cmd/Ctrl+K 命令面板、低饱和中性灰 + 单一主色、细边框替代厚重阴影、高密度信息 + 留白平衡、150–200ms 微交互、深浅色完整适配、全键盘操作友好；

2. Perplexity 借鉴点：正文数字 / 引用可点击上标角标，hover / 点击弹出来源详情卡片，页面顶部来源汇总条；

   

   整体气质：冷静专业、极简克制、高可信度科研工具，摒弃花哨娱乐化动效。

## 1. 设计令牌（Design Tokens）

### 颜色体系（浅色默认，深色镜像）

#### 主色

主强调：科学蓝 `#2563EB`；hover 态 `#1D4ED8`；深色模式 `#1E40AF`

#### 中性色（浅色）

背景 `#F8FAFC` / 卡片 `#FFFFFF` / 边框 `#E2E8F0` / 分割线 `#EEF2F6` / 正文 `#0F172A` / 次要文字 `#64748B` / 占位 `#94A3B8`

#### 语义状态色（全站统一）

可复现 / 校验通过：绿 `#16A34A`；漂移 / 错误告警：红 `#DC2626`；运行中 / 待校验：橙 `#F59E0B`；普通信息：蓝 `#2563EB`

#### 深色模式基底

背景 `#0B0F17` / 卡片 `#111827` / 边框 `#1F2937` / 正文 `#E5E7EB`

### 字体规范

1. 界面正文：Inter + 系统无衬线；中文 fallback 思源黑体；
2. 代码 / 哈希 / 数值等等宽内容：JetBrains Mono；
3. 字号阶梯：12 / 13 / 14 (正文基准) / 16 / 20 / 24 / 30；行高统一 1.5；标题字重 600，常规文本 400/500。

### 间距、圆角、层级（8px 栅格基准）

- 间距刻度：4 / 8 / 12 / 16 / 24 / 32 / 48
- 圆角：卡片 12px；按钮 / 输入框 8px；标签 / 角标 6px
- 阴影（克制使用，优先边框分隔）：
  - 常规卡片：`0 1px 2px rgba(15,23,42,.06)`
  - 弹窗 / 浮层：`0 8px 24px rgba(15,23,42,.12)`

### 动效令牌

常规过渡 150ms ease-out；页面入场 200ms；校验结果（绿勾 / 红叉）强调动效 300–400ms 弹性曲线。

## 2. 全局 UX 强制原则（全页面通用）

1. 溯源随手可达：所有结果卡片、结论区域右上角常驻「查看来源」按钮；正文数字、文献引用永久为可点击角标；
2. 流式实时反馈：检索、代码分析、质检全流程流式分步渲染（规划→思考→代码→产物），禁止空白加载等待；
3. 三态完整：所有数据模块必须实现空状态（操作引导）、骨架加载态（不使用旋转 spinner）、错误态（可读报错 + 重试 / 编辑按钮）；
4. 强视觉状态反馈：复现、漂移、校验失败使用专属动效收尾；
5. 键盘优先：Cmd/Ctrl+K 全局命令面板，核心操作绑定快捷键；
6. 信息克制：单页面仅保留 1 个核心任务，次要功能折叠抽屉，减少冗余按钮；
7. 语义色统一：绿 = 可信可复现，红 = 漂移 / 错误，橙 = 执行中，全站无歧义；
8. 非阻塞后台任务：入库、长时分析后台异步执行，前端持续可交互；

## 3. 全局外壳布局（Linear 标准布局）

plaintext









```
┌───────────────────────────────────────────────────────────────┐
│ 顶栏(高48)  ▪Logo ReproLab   [项目切换▾]      ⌘K 搜索    ◐主题 👤 │
├──────────┬────────────────────────────────────────────────────┤
│ 侧栏(w220)│                                                     │
│ ◈ 工作台   │                                                     │
│ ◈ 知识库   │                  主内容区                            │
│ ◈ 分析对话 │              (max-w 容器, 24px padding)              │
│ ◈ 溯源图谱 │                                                     │
│ ◈ 写作面板 │                                                     │
│ ─────────  │                                                     │
│ ◈ 记忆/设置│                                                     │
│ [模型:混元▾]│                                                     │
└──────────┴────────────────────────────────────────────────────┘
```

布局细节：

1. 左侧侧边栏：图标 + 文字组合；选中项左侧 2px 主色竖线 + 浅底色；底部常驻 LLM 模型切换下拉；
2. 顶部导航栏：项目切换、全局搜索入口、深浅色切换、用户头像；
3. 主内容区最大宽度 1200–1280px 居中对齐，组件间距严格遵循 8px 栅格。

## 4. 逐页面线框、组件、状态定义

### P1 工作台（首页，M10/M5 展示）

plaintext









```
┌ 项目概览 ────────────┐ ┌ 主动科研建议 (M10) ──────────┐
│ 文献 12  数据 5       │ │ 💡 建议1  …可回溯 ⟦src⟧       │
│ 分析 23               │ │ 💡 建议2  …可回溯 ⟦art⟧       │
│   ◐ 可复现率 86%      │ │ 💡 建议3                      │
│   (环形进度, 绿)      │ └──────────────────────────────┘
└──────────────────────┘ ┌ 最近分析 ────────────────────┐
                          │ • 企鹅体重检验  ✓可复现 2h前  │
                          │ • PCA 降维      ⚠漂移  1d前   │
                          └──────────────────────────────┘
```

页面要点：

- 招牌 KPI：环形绿色可复现率进度卡片，首页核心视觉；
- 每条智能科研建议附带可点击证据角标，回溯原始文献 / 数值产物；
- 空状态：无项目时展示上传数据 / 文献引导卡片。

### P2 知识库页（M1/M2，Perplexity 来源风格）

plaintext









```
┌ 顶部大搜索框  [关键词 | 语义 | 混合]  🔍 ────────────────────┐
├ 筛选: 类型▾ 标签▾ 年份▾ ─────────────────────────────────────┤
│ ┌文件卡┐ ┌文件卡┐ ┌文件卡┐   ← 网格, 拖拽上传高亮整区        │
│ │📄论文 │ │📊CSV │ │🐍code│                                  │
│ └──────┘ └──────┘ └──────┘                                  │
└─────────────────────────────────────────────────────────────┘
```

交互规则：

1. 搜索三模式切换：关键词 / BM25、语义 / 向量、混合融合检索；
2. 拖拽上传：整个文件网格区域拖拽文件，虚线高亮提示松手上传；
3. 检索结果：命中文本高亮，每条卡片附带「定位原文」按钮；点击文件右侧滑出预览抽屉 Sheet；
4. 加载态：文件卡片骨架占位；入库中卡片显示橙色进度角标。

### P3 分析对话页（核心页面，M3，三栏流式布局）

plaintext









```
┌ 会话/数据集(w260) ┬ 对话流(flex) ────────────┬ 产物预览(w360) ┐
│ ▸ 数据集          │ 用户: 画箱线图并检验       │ [图/表/数字]    │
│   penguins.csv    │ ─ plan  ▸3步              │  当前选中产物   │
│ ▸ 历史会话        │ ─ thinking …(可折叠)      │  [查看来源▸]    │
│                   │ ─ code  ```py```(可编辑▸重跑)│ [重跑] [回写]  │
│                   │ ─ run   ●运行中… / ✓      │                │
│                   │ ─ 📊图卡  ⟦art_0a1f⟧      │                │
│                   │ 结论: …系数0.083⟦art⟧,p<.01│                │
│                   │ [输入框  ⌘↵发送]          │                │
└───────────────────┴──────────────────────────┴────────────────┘
```

流式渲染顺序（SSE 推送）：规划步骤条 → 可折叠思考文本 → 高亮代码块（支持编辑重跑）→ 执行状态（橙色脉冲运行中 / 绿色通过 / 红色失败）→ 产物卡片 → Markdown+katex 带锚点结论；

功能细节：

1. 产物卡片右上角固定「查看来源、重跑、回写报告」操作；
2. 代码执行中：代码块顶部橙色脉冲进度条；
3. 空状态：页面中央引导文案 + 示例 Prompt 快捷芯片；
4. 失败态：红色 stdout 报错摘要，附带自动修复重试、手动编辑代码按钮。

### P4 溯源图谱页（招牌页面，M6，reactflow 全屏画布）

plaintext









```
┌ 顶部: [一键复现 ▸]   图例:●数据 ●代码 ●产物 ●结论 ●文献 ──────┐
│                                                                 │
│     ●penguins.csv ──reads──▶ ▢run(code_hash) ──produces──▶ ◆0.083│
│                                                        │supports │
│                                                        ▼         │
│                                                     ⬡结论        │
│  (可缩放/拖拽, 选中节点→右侧详情面板: 代码/环境/哈希/时间)        │
└─────────────────────────────────────────────────────────────────┘
```

交互规则：

1. 顶部一键复现按钮：执行后顶部横幅展示绿色一致 / 红色漂移，漂移时右侧滑出差异面板；
2. 点击结论节点：上游血缘链路流水式高亮（节点 / 边依次动画点亮）；
3. 节点语义着色，选中节点弹出右侧详情抽屉，展示完整哈希、环境快照、代码原文。

### P5 写作面板（M8，双栏编辑预览）

plaintext









```
┌ 编辑(Markdown+LaTeX) ────────┬ 预览 ─────────────────────┐
│ 数字化…系数 0.083⟦art_0a1f⟧, │ 数字化…系数 0.083⁴ᵃ, p<.01 │
│ 与研究一致⟦src_9c2e⟧。       │ 与研究一致⁹ᶜ。            │
│ [插入分析结果▾] [插入引用▾]  │ ← 锚点渲染为可点上标        │
│                              │ ⚠无法溯源项: 红色波浪下划线 │
└──────────────────────────────┴───────────────────────────┘
```

核心交互：点击「运行校验」，预览页面内不合格锚点标红，hover 展示质检失败原因。

### P6 记忆 / 设置页面（M9/M11，模型路由配置）

1. 长期记忆列表：分层标签（情节 / 语义）、写入时间戳、编辑入口，提示「使用前自动核验」；
2. 技能库面板：按学科分组技能卡片，新增技能注册按钮；
3. LLM 模型路由配置：全局默认模型（混元），支持编排 Agent、质检 Agent 独立选择强推理模型，实现架构模型无关。

## 5. 核心演示动效（framer-motion）

1. 复现结果：进度条完成；匹配一致 = 绿色对勾弹性缩放 (0.8→1.1→1)；漂移 = 红色叉号左右抖动 ±4px，卡片红边框，差异面板从右侧滑入；
2. 溯源链路高亮：点击结论节点，上游节点 / 边间隔 80ms 依次主色描边点亮；
3. 校验错误标红：不合格锚点下划线 200ms 平滑过渡为红色，hover 悬浮提示失败理由；
4. 产物卡片入场：透明度 0→1，纵向位移 8px→0，200ms 过渡。

## 6. 全站统一锚点渲染规范（Perplexity 风格）

1. `⟦art_xxxx⟧`：蓝色可点击上标；弹窗展示产物完整血缘链；
2. `⟦src_xxxx⟧`：灰色可点击上标；弹窗文献卡片，支持跳转原文段落；
3. 校验失败锚点：红色角标 + 悬浮提示；
4. 无锚点裸数字：预览页红色波浪下划线标记；
5. 统一封装组件：`components/anchor/AnchorChip.tsx`，对话、写作、问答页面复用。

## 7. shadcn/ui 组件落地映射清单

### 全局外壳

Sidebar（自定义扩展）、Command（全局⌘K 搜索面板）、DropdownMenu（项目 / 模型下拉）、Sheet（右侧预览抽屉）

### 数据卡片展示

Card、Badge（语义状态标签）、Skeleton（骨架加载）、Progress、自定义环形进度、Table

### 对话模块

ScrollArea、Collapsible（折叠思考文本）、代码高亮块、Textarea 输入框

### 反馈提示

sonner Toast、Tooltip 悬浮提示、Dialog 确认弹窗

### 溯源图谱

reactflow 自定义着色节点组件

### 目录规范

业务组件存放 `frontend/components/`，按页面 / 领域分文件夹；接口请求封装至 `frontend/lib/`。

## 8. 响应式与无障碍规范

1. 主适配桌面屏幕≥1280px；宽度 < 1024px 时三栏布局折叠为 Tab 切换；
2. 色彩不可作为唯一状态标识，红绿状态搭配✓/✗图标；文本对比度满足 WCAG AA 标准；
3. 所有可交互元素拥有主色 focus 焦点环；弹窗、抽屉支持 ESC 快捷键关闭。